name = "mood-tracker-push"
main = "src/index.js"
compatibility_date = "2026-08-01"
workers_dev = true

# 20:00 KST = 11:00 UTC. 서머타임 없는 나라라 고정이면 된다.
[triggers]
crons = ["0 11 * * *"]

[vars]
VAPID_PUBLIC_KEY = "BLOYnpEHpH7raOxMsiDc9ka3QtyfTaa-YcP16J1aqrvSWpkZosBvFhXXwOHpUIOHw134ZVXCZacFEusOzDPflZA"

# 사진 분석 제공자. 무료 한도가 없어지거나 품질이 아쉬우면 "anthropic"으로 바꾸고
# ANTHROPIC_API_KEY secret을 넣으면 된다. 앱은 건드릴 필요 없다.
AI_PROVIDER = "gemini"
# 모델명이 바뀌어 404가 나면 여기만 고친다.
GEMINI_MODEL = "gemini-3.6-flash"

# 아래 둘은 secret으로 넣는다 (공개 저장소라 파일에 두지 않는다):
#   npx wrangler secret put VAPID_PRIVATE_KEY
#   npx wrangler secret put VAPID_CONTACT_EMAIL
#   npx wrangler secret put GEMINI_API_KEY        (사진 분석용)
#
# KV 네임스페이스는 배포 전에 만들고 아래 id를 채운다:
#   npx wrangler kv namespace create SUBS

[[kv_namespaces]]
binding = "SUBS"
id = "3c69e4ea81db46a9907e58dd79143326"
